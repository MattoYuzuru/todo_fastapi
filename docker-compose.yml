services:
  postgres:
    image: postgres:15-alpine
    container_name: todo_db
    restart: always
    env_file:
      - .env_db
    ports:
      - "5430:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: